# Generation progress UI

The 3D viewport stays hidden while text-to-3D is running. Users see pipeline progress instead. The textured GLB viewer, world-position controls, SceneSpec download and FiveM export only appear after the final model reaches `success`.
